package xfsgo

// import "math/big"

// type Validator interface {
// 	// ValidateBody validates the given block's content.
// 	ValidateBody(block *Block) error

// 	// ValidateState validates the given statedb and optionally the receipts and
// 	// gas used.
// 	ValidateState(block, parent *Block, state *StateTree, receipts []*Receipt, usedGas *big.Int) error
// 	// ValidateDposState validates the given dpos state
// 	ValidateDposState(block *Block) error
// }
